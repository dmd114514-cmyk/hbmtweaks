# HBM Tweaks

**微调 HBM 核科技里的每一个数值——逐弹种、逐枪、逐怪、逐装备。你的整合包，你的数值。**

HBM Tweaks 是一个面向 Minecraft **1.12.2** 上 **HBM 核科技 - 社区版（NTM-CE）2.5.0.5** 的配置驱动型数值调整附属模组。它把 HBM 的内部数值以一份清晰、带完整文档的 JSON 配置暴露出来——整合包作者（以及喜欢折腾数值的玩家）可以重平衡任何东西，**无需改动一行 Java**。

**一句话版本：** 安装 → 打开 `config/hbmtweak/hbmtweaks.json` → 改一个数字 → 重启。没有任何预设数值，模组本身在你配置之前**什么都不改**。

---

## ✨ 核心功能

| | |
|---|---|
| 🎯 **157 个逐弹种配置** | 伤害、穿甲、护甲阈值穿透、击退、爆头倍率、散布、弹速、穿透——每种子弹独立调整 |
| 🔫 **逐枪覆写** | 解决同口径共享倍率的偏差（比如多把枪共用一颗 .22 子弹），按整把枪覆写基础伤害 |
| 💣 **手榴弹** | 万能手榴弹全部 13 种装药（火药 → 核装药）外加炸药棒 |
| 👹 **怪物** | 血量（倍率**或绝对值**）、造成伤害、受到伤害、速度、击退抗性、单次受伤上限、火焰/魔法免疫 |
| ⚔️ **近战与工具** | 剑、镐、斧、铲、电锯、扳手——**物品标签与实际伤害永远一致** |
| 🏰 **炮台** | 锁敌范围、耗电、转速，以及直接伤害（陶子炮/微波炮）倍率 |
| 💥 **爆炸** | 炮击炮弹（Arty / HIMARS）爆炸伤害 |
| 🍞 **食物** | 任意物品的饥饿值与饱和度 |
| ☢️ **核弹** | BombConfig 全部字段（爆炸半径等），由开关安全接管 |
| 📖 **自动 ID 参考** | 每次启动自动重新生成 `hbm_tweaks_ids.txt`，列出全部可调整 ID |

---

## 🧭 设计原则

1. **默认 = 原版。** 每个开关默认关闭、每个倍率默认 1.0。装上去什么都不变。
2. **零硬编码。** 模组对"你应该用什么数值"没有任何意见。
3. **模组 ≠ 整合包。** 模组是通用工具，你的数值存在*你的*配置里。更新模组永远不动你的平衡。
4. **显式优于隐式。** 每个配置值都是*最终值*——没有隐藏计算、没有意外倍率。
5. **构造安全。** 所有 Mixin 注入都是 `required=false`；即使未来 HBM 更新导致某项失效，也只有那一项静默降级。游戏永远不会因为 HBM Tweaks 崩溃。

---

## 📦 安装

1. **依赖**
   - Minecraft **1.12.2**（Forge **14.23.5.2864+**）
   - **HBM 核科技 - 社区版 2.5.0.5**（必需）
   - **MixinBooter**（Mixin 功能必需）
2. 把 `hbmtweaks-1.3.0.jar` 放进 `mods/` 文件夹。
3. 启动一次游戏。模组自动生成：
   - `config/hbmtweak/hbmtweaks.json` —— 你的配置文件
   - `config/hbmtweak/hbm_tweaks_ids.txt` —— 全部可调整 ID，自动更新
4. 编辑配置，重启游戏。完成。

> ⚠️ **请使用 UTF-8 无 BOM 编辑器**（VS Code、Notepad++ 等）。JSON 开头的 BOM 会导致游戏回退默认值。

---

## ⚙️ 配置快速上手

```jsonc
// config/hbmtweak/hbmtweaks.json
{
  // 12号霰弹伤害减半
  "weapons": [ { "bulletName": "g12", "damageMult": 0.5 } ],

  // AM180：基础伤害 ×5
  "guns": [ { "item": "hbm:gun_am180", "damageMult": 5.0 } ],

  // UFO：血量固定为 100（绝对值——无需反算）
  "mobs": [ { "entityClass": "com.hbm.entity.mob.EntityUFO", "healthSet": 100 } ],

  // 陨石剑：每次攻击恰好 4 点（标签同步显示）
  "tools": [ { "item": "hbm:meteorite_sword", "damageSet": 4 } ],

  // HE 装药手榴弹：爆炸 ×0.2
  "enableGrenadeOverride": true,
  "grenades": [ { "filling": "HE", "damageMult": 0.2 } ],

  // 面包：恢复满饥饿值
  "foods": [ { "item": "minecraft:bread", "hunger": 20 } ]
}
```

**处处支持两种数值写法：**
- `damageSet` / `healthSet` —— **绝对目标值**（写 `4` 就是 4 伤害，零计算）
- `damageMult` / `healthMult` —— **倍率**（写 `0.5` 就是减半）

每个类别在配置里都有 `"_xxx_usage"` 注释说明字段含义，`hbm_tweaks_ids.txt` 列出全部有效 ID。

---

## ❓ 常见问题

**Q: 这个模组装上会改东西吗？**
不会。所有开关默认关闭、所有倍率默认 1.0。它是一把空白的工具，等你配置。

**Q: 我改了数值但没生效？**
- 重启游戏了吗？所有数值在启动时加载。
- 用 `hbm_tweaks_ids.txt` 核对弹种/物品/类名（拼写错误会打出警告日志）。
- 怪物的 `entityClass` 必须与*运行时*类名完全一致——子类需要单独条目。

**Q: 我的剑上标签还是显示旧数值？**
近战工具是原地修改的，标签与实际一致。如果你说的是**枪**——枪械标签显示"基础伤害 × 弹药倍率"，会正确反映 `guns`/`weapons` 配置。

**Q: HBM 更新会不会弄坏它？**
个别功能可能失效，但绝不会致命：失败的注入会静默降级（记入警告日志），其余功能照常工作。

**Q: 它会改动 HBM 的文件吗？**
不会。一切都在运行时通过反射、Forge 事件和 Mixin 完成。HBM 的 jar 从不被修改。

---

## 📜 更新日志

**1.3.0**
- 模组/整合包彻底分离：全部数值配置化，所有开关默认关闭
- `healthSet`（怪物血量绝对值）、`damageSet`（近战伤害绝对值）
- 近战物品标签与实际伤害一致（原地属性编辑）
- 手榴弹逐装药覆写；炮台直接伤害与炮弹爆炸倍率
- 逐枪、数组弹种（火箭）、手榴弹与近战缓存；自动 ID 文档

**1.2.0**
- 逐枪伤害、怪物造成伤害倍率、炮台/炮弹/手榴弹压缩、glyphid 与 BOT Prime 支持、Mixin 扩展

**1.1.0**
- 怪物伤害上限、弹道/爆炸倍率、火焰/魔法免疫、炮台 Mixin、配置模板

**1.0.0**
- 初版：弹种、旧枪械、怪物、食物、核弹、炮台属性

---

## 📄 许可与致谢

**HBM Tweaks 以 MIT 许可证开源。** [源码](https://github.com/dmd114514-cmyk/hbmtweaks) 托管在 GitHub。

这是一个**独立附属模组**，不是 HBM 官方项目。它在运行时基于 HBM 核科技 - 社区版的开源许可（GPL/LGPL）进行互操作，且**不含任何 HBM 代码副本**。基础游戏的所有功劳归于 HBM 模组团队与 NTM-CE 社区。

---

## 💬 支持与反馈

遇到 Bug 或有数值平衡问题？请在评论区附上：
- `logs/latest.log`（游戏目录下）
- 你的 `hbmtweaks.json` 相关片段

祝你调数愉快！
